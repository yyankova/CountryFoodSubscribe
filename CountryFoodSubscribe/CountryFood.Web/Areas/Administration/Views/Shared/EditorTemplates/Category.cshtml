@(Html.Kendo().DropDownList()
          .Name("Category")
          .DataTextField("Name")
          .DataValueField("Id")
          .OptionLabel("Select category...")
          .DataSource(dataSource => dataSource
                       .Read(read => read.Action("ReadCategories", "Categories"))
                 )
)

